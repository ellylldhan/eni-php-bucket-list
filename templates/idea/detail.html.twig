{% extends 'layout.html.twig' %}

{% block title %}B-List | Details{% endblock %}

{% block main %}
    <h1>Le Détail d'une Chose de la Liste du Seau !</h1>
    <div>
        <h3>{{ idea.title }}</h3>
        <p>{{ idea.description }}</p>
        <p><i>{{ idea.author }} - {{ idea.dateCreated | date("d/m/Y @ H:i") }}</i></p>
    </div>
{% endblock %}

